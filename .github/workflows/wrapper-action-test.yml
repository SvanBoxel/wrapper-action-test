name: test wrapper action
on:
  push:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - name: test-action
      uses: SvanBoxel/parameterizable-docker-action-example@master
      with: 
        alpine-version: 3.6

    - name: validate output
      run: ${{steps.test-action.outputs.version}}
